# secrets/ directory
# All files here are Docker secrets (mounted at /run/secrets/ in containers).
# NEVER commit *.txt files in this directory.
# See scripts/init_secrets.sh to generate placeholder files.
